using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyThatCube : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //poka kubik
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
